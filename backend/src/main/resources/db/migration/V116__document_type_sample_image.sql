-- 서류 수집 '샘플 보기': 서류종류별 마스킹된 예시 이미지 1장의 스토리지 키.
-- ADMIN 이 이미 마스킹한 예시를 수동 업로드 → 수집 링크를 받은 대상자(차주·인력원)가
-- 업로드 화면에서 "이런 걸 올리면 됩니다" 예시를 보고 무엇을 올릴지 즉시 인지.
-- 시스템은 마스킹하지 않는다(저장·표시만). NULL = 샘플 미등록.
ALTER TABLE document_types
    ADD COLUMN sample_image_key VARCHAR(255);
