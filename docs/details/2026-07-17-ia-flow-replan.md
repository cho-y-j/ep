# SKEP v2 전면 간소화·통합 기획서 (초안 v1)

> 2026-07-17 · 작업 1(기획=페이블, 구현=오퍼스) · 근거: 코드 실사 리포트(파일:라인 전수 확인) + 고용노동부 폭염 기준(2025.7.17 시행 산업안전보건기준 규칙)
> 상태: **사용자 승인 대기 초안**

## 0-A. UX 대원칙 (2026-07-17 사용자 — 전 화면 적용, 어드민 포함)

1. **무조건 사용이 편해야 한다** — 클릭 최소, 역할별 첫 화면 = "오늘 내가 할 일"이 바로 보임.
2. **보기 좋은 디자인 + 차트 대시보드** — 표 나열이 아니라 카드·차트로 **한눈에 상황 파악**.
3. **작업자도 1급 사용자** — 운전원·신호수 등 **역할에 맞는 앱 동작** + **내가 일한 내역(작업확인서 기반 일일/월간, 서명 상태, OT 시간)을 앱에서 본인 확인** 가능.
4. **위치·안전·혼잡도 대시보드** — 작업자 위치(구역 단위)·안전 상태·**시간대별 혼잡도**를 관제 차트로 → 점심시간 조정 등 현장 계획의 데이터 자산화. (정밀 실내측위는 비콘 없인 한계 — 구역(공구·레벨) 단위 분포는 출근·지오펜스·작업위치 기록으로 가능, 정직 표기)

## 0. 한 줄 목표

BP가 **웹에서 서류를 바로 열어 승인하고, 그 자리에서 작업계획서와 검사·교육·검진 날짜 통보까지** 끝내는 원스톱 흐름을 만들고, 메뉴를 역할당 5허브로 재편하며, 견적·서류·투입·정산이 **각각 단독으로도** 돌아가게 한다.

---

## 1. 용어·관계 정의 (공급사 관리자 ↔ BP사 R&R)

"작업 관리자" = **공급사(장비업자·인력업자) 쪽 관리자**.

| 단계 | 공급사 관리자가 할 일 | BP사가 할 일 |
|---|---|---|
| 자원 준비 | 장비·인력 등록, 서류 업로드(OCR·진위검증), 만료 관리 | (필요서류 기준 제공 = 현장 프로필) |
| 견적 | 견적요청 확인 → 다항목 견적 제출 or 기성 견적 첨부·발송 | 견적요청 발행, 비교·선정·확정 |
| 서류 심사 | 심사 세트(장비+인력) 발송 — **이 시점에 실자원 지정** | **웹에서 열람→승인/반려**, 승인 즉시 후속 액션 |
| 투입 준비 | 반입검사·건강검진·안전교육 일정 수행 | 작업계획서 작성, 검사·검진·교육 **날짜 통보**(알림톡+앱/웹) |
| 투입·운영 | 배정 관리, 작업자 앱 출근 독려 | 작업확인서 서명(현장 담당), 안전 모니터링 |
| 정산 | 월간 작업확인서·거래명세서 확인 | 단가조건 정산 승인 |
| 안전 | 자사 작업자 이상 알림 수신·조치 | 현장 안전규칙 설정(온도·휴식), 이상 알림 수신·조치 |

### 1.1 주체 확장 — 4주체 → 6주체 (2026-07-17 사용자)

| 주체 | 시스템 현황 | 필요 |
|---|---|---|
| 원청(SK) 현장 관리자 | **계정·화면 없음** — client_orgs 데이터만 존재, 작업계획서 서명자 5명 중 4명(SKEP 관리감독자·HYPER·안전·시공팀장)이 원청측인데 이메일 토큰 서명만 | **원청 역할 신설 + 현장 통합 모니터링 허브**: 내 현장의 BP 목록·작업회사(공급사)·직원·장비, 출근 현황, 일일점검(조종원+법정점검 2종), 안전알림(폭염·강풍·워치), 서류/만료 리스크 — 읽기 전용 관제. (추후 서명도 인앱 수신 가능) |
| **안전점검회사** | 없음 | **회사 유형 신설**(안전점검 전문사) + 소속 점검원 관리 + S2′ NFC 법정점검 수행 주체 + 점검원 화면(오늘 점검대상 장비·완료 현황·서명) |
| BP / 장비공급사 / 인력공급사(+협력사) / 작업자 | 기존 | (본 기획서 각 절) |

## 2. As-Is 진단 (실사 근거 요약)

| # | 문제 | 근거 | 판정 |
|---|---|---|---|
| A1 | BP 받은심사 = **zip 다운로드 전용**. 승인/반려·인라인 뷰어·만료/검증상태 전부 없음 | `BpReceivedReviewsPage.tsx:67-81`, `DocumentReviewInboxController.java:23-57` | 사실 |
| A2 | 원하는 UX는 ADMIN 검토큐에 **이미 구현**(blob 뷰어+승인/반려+정부API 결과) → 이식 가능 | `ReviewQueuePage.tsx:396-541` | 기회 |
| A3 | 선결 갭: 심사봉투에 문서 id 목록 없음 / BP 파일열람 권한이 '사이트 ACTIVE 참여'에 묶여 심사 수신을 무시 | `DocumentReviewInboxService.java:134-141`, `DocumentService.java:665-677` | 사실 |
| B1 | 받은심사→작업계획서 진입점 없음(견적/서류관리에서만). 점검요청 다이얼로그는 계획서 상세 전용이나 **workPlanId optional이라 재사용 가능** | `WorkPlanDetailPage.tsx:562`, `IssueResourceCheckDialog.tsx:16`, `ResourceCheckService.java:70-73` | 사실 |
| B2 | 알림톡 템플릿 3종뿐(건강검진·차량안전점검·장비견적) — **안전교육 날짜통보 템플릿 부재**(다온톡 콘솔 등록 = 외부 작업) | `AlimTalkTemplate.java:7-67` | 사실 |
| C1 | 서류심사·작업계획서·투입요청·투입대기(readiness)는 **이미 견적 무관** — 재작업 불필요, IA 동선만 노출 | `DocumentReviewMailService.java:57`, `WorkPlanService.java:182`, `FieldDeploymentService.java:59`, `ResourceReadinessService.java:31-43` | 좋은 소식 |
| C2 | 진짜 결합: **배차=견적 FINAL_ACCEPTED 필수** + **정산 원천=배차 행만** → 견적 밖 투입(FieldDeployment)은 정산·투입보드에 안 잡힘 | `DispatchedEquipmentService.java:74-79`, `SettlementService.java:46` | 핵심 |
| C3 | 견적 다항목 불가: 번들도 장비 **단수 1항목**(인력만 N) | `CreateQuotationBundleRequest.java:26-27` | 사실 |
| D1 | 심사 메일 수신자 = 공급사가 입력한 메일뿐, **BP 담당자 자동 CC 없음**. Company에 email 없음 → User.email로 해결 | `DocumentReviewMailService.java:57-183`, `User.java:21-22` | 사실 |
| E1 | 메뉴 실측: **ADMIN 26 · BP 27~28 · 공급사 22~24** | `Sidebar.tsx:55-167` | 사실 |
| F1 | 안전 엔진 **대부분 실재**: KMA 날씨→체감온도→단계(31/33/35/38 하드코딩), 매시 45분 휴식알림 스케줄러, 워치 바이탈/응급 수신, WebSocket 관제, Site 지오펜스 반경 | `HeatStage.java:13-18`, `HeatRestScheduler.java:48-90`, `FieldSafetyController.java:22-100`, `Site.java:56-73` | 좋은 소식 |
| F2 | 갭: **현장별 설정 저장 구조 없음**(임계·간격 하드코딩), Site에 원청(clientOrg) 차원 없음 | `Site.java:56-73` | 사실 |
| G1 | 서류 junction(장비종류×서류·인력역할×서류)은 전역 단일 — 헬퍼 서비스 2개가 좁은 허리라 **현장 override 병합으로 국소 확장 가능** | `EquipmentDocRequirementService.java:27-42`, `PersonDocRequirementService.java:12-40` | 기회 |

## 3. To-Be 흐름 재설계

### 3.1 BP 원스톱 심사 (P0 핵심)

```
받은 심사함(봉투 목록, 배지)
 └▶ 봉투 상세: 자원별 문서 리스트 + [인라인 뷰어: 이미지/PDF] + 만료일 + 정부API 검증상태
      ├▶ [승인] / [반려(사유)]  ← DocumentReview 상태머신 신설(심사중→승인/반려)
      └▶ 승인 직후 그 자리에서:
           ├▶ [작업계획서 만들기] → /work-plans/new?siteId=&supplierId= 프리필
           └▶ [검사·교육·검진 날짜 통보] → IssueResourceCheckDialog 직접 호출
                (반입검사/건강검진/안전교육 + 날짜 + 알림톡, 계획서 없이도 발행 가능)
```
선결 3종: ① 봉투→문서목록 API ② `ensureCanAccess`에 "내 앞으로 온 심사 문서" 열람 분기 ③ DocumentReview 상태 컬럼. 뷰어·승인 UI는 ADMIN 검토큐 패턴 이식(신규 발명 없음). 메일은 BP 회사 사용자 이메일 **자동 CC**.

**심사 발송 3채널 (2026-07-17 사용자 — "현장에서 바로 사용" 필수)**: ①웹 수신함(P0) ②메일 = 첨부 + **"웹에서 바로 확인" 링크** + CC ③**알림톡 통지**("메일 보냈으니 확인" — 공급사 발송 즉시 BP 담당자 카톡). 알림톡 템플릿 3종(심사도착·반입검사 날짜·안전교육 날짜) 다온톡 콘솔 등록 = 사용자 액션, 문구 초안 제공됨. 견적 없이 서류심사 단독 시작이 1급 동선(재확인).

### 3.2 디커플링 (견적/서류/투입/정산 각자 단독) — 2026-07-17 사용자 확정

- **확정 원칙**: 견적→심사→투입 전체 연결은 이상적 경로로 **그대로 유지**하되, 현장 현실(견적은 견적대로, 실제는 서류심사부터)에 맞춰 **서류심사부터 시작하는 경로를 1급 동선**으로 노출. **견적서는 업체 선정의 근거자료로 보존**(선정 이력에서 참조 가능해야).
- 이미 단독 가능: 서류심사·작업계획서·투입요청·투입대기 → IA에서 "견적 없이 시작" 동선 노출.
- 신규: **투입 확정(FieldDeployment.accept) 시 정산 원천 행 생성** → 견적을 안 거친 투입도 투입보드·정산에 잡힘. (`FieldDeploymentService.accept:131` 훅)

### 3.3 견적 = 템플릿(단가표) 방식 (2026-07-17 사용자 확정)

- 현실: BP 요청은 "지게차 전체 견적 주세요 / 5톤 주세요" 수준으로 단순 → 요청 폼은 단순 유지.
- 공급사는 **견적 템플릿(단가표)**을 미리 등록 → 요청 수신 시 템플릿 불러와 단가 확인·수정 후 발송. 또는 기존 견적서 **파일 첨부** 발송(현행 유지).
- **라인 필드 확정(2026-07-17 사용자)**: 장비종류·규격(톤수/m)별 행 = **기본단가(일대/월대) + OT 5분류 단가(조출·점심·연장·야간·철야)**. (견적서 실양식 수령 시 레이아웃만 추가 확인)

### 3.3.1 ★계약(Contract) 단계 신설 — 단가의 진짜 원천 (2026-07-17 사용자 교정)

**현장 실제 흐름**: 전화 견적요청 → 견적서 메일(참고) → **현장 소장과 계약서 작성 = 단가 확정 시점** → 투입 날짜 잡히면 서류 발송 → BP 작업계획서 → 투입 지시(날짜) → 투입 → 정산(계약 단가).

- 시스템에 "계약" 단계가 없어 단가 확정의 앵커가 부재했음 → **계약 레코드 신설**: BP↔공급사·현장·장비종류/규격·**5분류 단가 세트**·기간·계약서 파일(스캔 첨부).
- 생성 경로 2가지(디커플링 원칙): ⓐ 시스템 견적에서 선정 시 자동 생성(견적 링크=근거자료) ⓑ **전화/메일 견적이었어도 계약만 직접 등록**(견적 없이 — 현장 대부분의 케이스).
- 정산·투입 단가는 **계약에서** 온다(견적 스냅샷 아님). 투입 지시(날짜 통보) 단계도 흐름에 명시.

### 3.6 ★작업계획서 자동화 (핵심 가치 — 2026-07-17 사용자 통찰로 승격)

**문제의 본질**: 차량·인력이 바뀔 때마다 BP가 작업계획서를 새로 써야 해서 **기존 업체를 못 바꾼다**(전환비용=락인). 이 프로그램의 결정적 가치 = 이 전환비용 제거. 서류검증·OCR로 자원 데이터가 이미 시스템에 있으므로 실현 가능성 높음.

- **L1 자동 생성**: 등록 자원 데이터(OCR 추출·검증된 차량번호·규격·면허·보험·검사일)+현장 정보로 계획서를 자동 채움 — 수기 재입력 0. (P0의 "승인→계획서 프리필"이 진입점)
- **L2 교체 델타 — 현장 단위 규칙 (2026-07-17 사용자 확정)**:
  - **같은 현장 + 기검증 자원**(반입검사·인력관리 완료): **업체변경 신청서 한 장으로 재투입 가능** → L2a = 현장 내 기검증 자원 풀에서 교체 후보 선택 → **업체변경 신청서 자동 생성(DOCX)** → 서명 → 끝. BP가 기존 현장 자원을 선호하는 현실을 그대로 시스템화 — 신규 업체도 한 번 반입되면 동등해져 락인 해제.
  - **다른 현장이면 신규**(같은 SK라도 관리 현장이 다르면): 서류 심사→반입검사→안전교육 전부 다시 = L1 자동생성 + 검사·교육 일정 자동 요청으로 부담 최소화.
  - 교체 시점 셋 다 실재(당일 긴급 고장·결근 / 사전 계획 / 신규 업체 추가) → 긴급용 모바일 동선 + 사전검증(L3) 모두 필요.
- **주기 관리(신규 발견)**: 반입검사 **월 1회**·건강검진 **6개월** 재실시 — 주기가 현장마다 달라 **BP가 현장 프로필에서 설정**. 만료관리와 동일 패턴으로 재검·재검진 임박 알림.
- **L3 교체 사전검증**: 교체 후보 선택 시 서류·만료·점검(주기 포함) 게이트 자동 체크(readiness 재사용) → "바로 재투입 가능 / 반입검사 기한 초과 / 서류 2건 부족" 즉시 표시.
- 입력 대기: 업체변경 신청서 실양식(추후 — **그때까지 임의 양식 v0 사용**: 현장/적용일/변경구분(장비·조종원·업체)/변경 전후 표(자동채움)/사유/신규자원 확인 3체크(=L3 판정 자동)/신청(공급사)·확인(BP 소장) 서명. 실양식 수령 시 교체). 계약서 실양식도 요청됨.

#### 3.6.1 구현 설계 (2026-07-17 도메인 실사 반영 — 상세 근거는 실사 리포트)

**오늘 차 한 대 교체 시 실제 절차(코드로 실증한 전환비용)**: ①진행 중 계획서 취소 ②clone하되 **워크시트 내용(formValues) 전체 소실** → 공정·안전대책 처음부터 재입력 ③DRAFT에서 장비 제거→추가(컴플라이언스 게이트) ④**5서명 전부 재수집** ⑤재제출→승인→(새 장비 안전점검부터) ⑥정산 원천(dispatched)은 옛 차로 남음. — ②④⑥이 최대 비용.

**빌드 순서: L3 → L1 → L2** (실사 난이도 판정)
- **L3(난이도 하, 선행조건 없음)**: 자원 1건 교체 사전판정 API `GET /api/resources/{type}/{id}/replace-check?siteId=` — 기존 private 게이트 로직(서류 evaluateForClone + 점검/안전/이행 evaluate) 합성 래퍼 + 후보 UI 배지.
- **L1(중)**: 프리필 확장(Equipment 보험·검사만료 3필드 + Document.extractedData의 OCR 면허번호 배선) + **formValues 읽기 API**(현재 write-only) + 미배선 필드(capacity/spec/attach 명목 체크) 정리. 실양식 수령 후 DOCX 매핑.
- **L2(중 — 정책 확정으로 단순화)**: `POST /{id}/replace-resource` = clone 확장(formValues 복사→**자원 파생 키만 재계산**(실사 분류표) + `cloned_from_id` 1컬럼 + 원본 자동종료 연동). **부분 서명 무효화 불필요** — "전원 재서명" 규정이라 새 계획서=서명 0 시작(기존 clone 동작과 일치). 단 **기존 계획서 내용 변경 시 서버가 서명 전체 무효화** 강제 추가(현행: 클라 다이얼로그에서 '유지' 선택 가능 = 규정 위반 구멍). + L2a 업체변경 신청서 DOCX 자동생성(계획서 재서명 없음 — 신청서 자체 서명만).

**실사에서 발견된 기반 결함 2건 (자동화 전에 수정 필요)**
1. **formValues write-only**: 저장한 워크시트 내용을 응답에 안 담아 다시 읽을 경로가 없음 → 저장 후 이어서 수정 불가·clone 시 소실의 근원.
2. **서명자가 실제 문서를 못 봄**: 공개 서명 페이지 PDF가 서버 13필드 요약 렌더(워크시트 내용 미포함) → "무엇에 서명했나" 모호(컴플라이언스 위험). L1에서 서버 렌더를 formValues 기반으로 일원화 + 서명시점 스냅샷 보존 검토.

#### 3.6.2 실양식 분석 (2026-07-17 수령: `test/작업계획서_실양식_고소작업차.pdf`, 67p — 센코어테크/용인 Cluster/경기99사9489)

**패키지 구조 = 본문 ~5p + 첨부 ~62p(자원 서류 전량 병합)** — 첨부가 곧 시스템의 서류들:
- 본문: 표지(유형체크·장비식별·현장·기간·**5서명표=코드 SignatureRole 5역할과 정확 일치**·첨부16 체크리스트) → 1.사용계획(기종/성능/조종원/지반/전선/침하/아웃트리거/풍속/FailSafe) → 2.위험요인·안전대책 6행 → 3.작업계획도(도면) → 4.점검 8문 → 5.인적/교육(작업지휘자 지정서·보호구·신호방법·특별교육·서약)
- 첨부(확인분): 자동차등록원부(갑)·자동차등록증·사업자등록증·**조종원별 자격 묶음**(운전면허+기초안전보건+화물자격+고소작업대수료)×2인·교육실시확인서×2인·안전인증서·제원표+작업반경도·자동차보험 가입증명서·철판 시험성적서

**설계 확정 사항**:
1. **본문 = 현행 워크시트(132필드)와 동일 계열** — 5서명 역할·첨부16·위험6행·점검8문 전부 일치 → L1은 기존 템플릿 검증+미배선 필드 채우기.
2. **조종원 주야 2인 슬롯**(성명·면허취득일·교육이수일 ×2) — 사용자 확인: 주야 근무 2명 서류까지 첨부되는 사례. 스키마 operator 1인 전제 → 2인 확장 필요(WorkPlanPerson은 이미 N명 지원).
3. **L1 최대 가치 = 첨부 자동 조립**: 장비+조종원들의 검증된 서류를 표지 체크리스트 순서 규칙대로 자동 병합 → "67페이지 패키지 클릭 한 번". 엔진의 첨부 이미지 페이지 렌더(engine.ts:103-228) 재사용.
4. **본문 자동 채움 원천 실증**: 면허취득일=화물자격증 OCR값(이석구 2020.07.28 — 이미 우리 OCR·검증 통과한 그 필드), 성능 45m·아웃트리거 8.17m=제원표, 보험기간=보험증권, 검사유효=등록증. 
5. **신규 서류종류 후보**: 자동차등록원부(갑)·고소작업대 조종자격 수료증·교육 실시확인서(특수형태근로자)·장비 제원표(작업반경도 포함)·안전인증서·철판 시험성적서 — 장비종류×서류 체크리스트(기존 junction)에 추가만 하면 됨.

#### 3.6.3 작업확인서 실양식 분석 (2026-07-17 수령: `test/작업확인서.png` — 일일 전표 실물)

현장 실물 = 장비업체(대성스카이크레인) 전표 패드에 손글씨 → BP(센코어테크) 확인자 서명. **필드**: 현장명 / 기사명 / 협력사명(=일 시킨 시공사) / 차량번호 / 작업위치(구역좌표 예: X6/Y줄) / 장비명·규격(고소작업차 450) / **구분 체크(일대·월대·O/T·철야) + O/T 시간수 + 작업시간(07:00~21:00)** / 연장작업 내용 / 날짜 / **확인자 서명(BP 담당)** / 발행사 연락처.

**정산 흐름과 정확히 맞물림**: 구분(일대/월대)·OT 시간이 정산 재설계(V79 — ÷25·근무일수·OT단가)의 입력값 그대로. 설계 방향:
1. **일일 확인서 디지털화**: 작업자 앱 작업종료 시 전표 자동 생성(현장·기사·차량·시간은 시스템이 이미 앎 — 수기 항목은 작업위치·작업내용·OT시간 정도) → BP 담당자 모바일 서명 → 즉시 확정.
2. **월간 작업확인서 = 일일 전표 집계 자동 생성**(사용자 확정 흐름: 일일 서명 근거 → 월간 확인서 → 월간 거래명세서 → 정산). 기존 WorkConfirmation·월별 페이지·정산(V79)·명세서 스케줄러 인프라에 "일일 전표" 입력층만 보강.
3. 종이 대비 이득: 분실 없음·집계 자동·정산 다툼 감소(서명+시간 기록).

**월간 실양식 추가 분석 (`test/작업확인서 월간.png` — "2026년 01,02월 고소작업대(SKY) 작업확인서")**:
- 구조: 헤더(공사명·장비번호·운전원, BP/SKEP 확인란) + **일 단위 행 표**(날짜별: 작업내용, OT 시간수, **일별 확인 서명**) + 특기사항.
- **발견① 정산 주기가 달력월이 아니라 26일~익월 25일** — `Site.settlementDay`(현장정산일, V79에 이미 존재)와 정확히 일치. 월간 확인서 기간 = 현장정산일 기준으로 생성해야.
- **발견② OT 5분류·고정 시간대**: 조출(05-07)/점심(12-13)/연장(17-19)/야간(19-21:30)/철야(21-06) — 열별로 시간수 기입. 현행 정산의 단일 OT보다 세분 → 정산 단가체계에 OT 분류별 단가(또는 시간대 자동 판정) 필요 여부 검토.
- **발견③ 운영 실태(사용자 확정)**: 이 시트는 **현장 사무실 비치 종이 원장** — 작업자가 **매일 사무실에 직접 가서** 그날 행(작업내용·OT시간)을 손으로 적고 **그 자리에서 BP 확인 서명**을 받아 **현장(BP) 보관**. **1장짜리 일일 전표는 장비업체 보관용**(정산 청구 근거) — 같은 하루 사실을 양측이 각자 종이로 보관하는 이중 관행(사용자 확정).
- **디지털 대체 설계**: 작업자는 앱에서 작업종료 시 그날 기록 확정(사무실 왕복 제거) → BP 담당자는 웹/모바일에서 당일 행 서명(현장 어디서든) → 현장 원장은 **실시간 화면**(이 표 레이아웃 그대로: 현장정산일 주기 26~25·OT 분류 열·일별 서명 표시)이 됨. 종이 원장·업체 전표 둘 다 이 단일 기록의 뷰로 대체.
- 운전원 표기 1명(주간 시트) — 주야 2인 시 시트 분리 여부는 확인 필요(경미).
- **정산 계산식 확정(2026-07-17 사용자)**: OT 5분류 단가는 **견적 시 분류별로 각각 제시**되고 계약 성사 시 그 업체 단가 세트로 확정 → **정산 = Σ(일일 확인서의 분류별 시간수 × 계약 분류별 단가) + 기본(일대/월대)**. 견적 템플릿(§3.3)·투입 단가 스냅샷·정산(V79 확장)이 한 체인.

**정책 확정 (2026-07-17 사용자)**: ①계획서 재작성 시 **서명 전원(5명) 재수집** — 내용 변경=서버 강제 전체 무효화로 구현 ②**계획서 1건=장비 1대**(현 워크시트 구조 유지) ③면허번호 원천=**OCR 검증값**(Document.extractedData 파싱 계층 신설, 수기 qualification은 폴백) ④진행중 교체=**새 계획서 자동생성+원본 자동종료**(cloned_from_id 이력 연결, 상태기계 무손상). 잔여 조율: 정산 연동 순서(§3.2 디커플링과 함께).

### 3.4 안전 알림 엔진 = "현장별 설정" 한 겹만 추가

- 기본값(법정, 이미 코드와 일치): 31℃ 주의(240분)/33℃ 2시간 20분 휴식/35℃ 매시 15분+14~17시 옥외중지/38℃ 긴급 외 중지. +근로기준법 4시간→30분 휴게.
- 신규: `site_safety_settings`(단계별 임계온도·휴식간격/시간·무더위시간대·중지단계·수신채널) + BP 알림설정 UI + **법정 기준보다 완화 금지 가드**. `HeatStage.of()`/`intervalMinutes()`를 현장 설정으로 오버라이드.
- 채널: 작업자 앱·워치 + BP/공급사 관리자 웹·알림톡(38℃ 중지급은 관리자 동시 통보).

### 3.5 현장 프로필 (발주사별 차이 — 확정안)

- 프로그램 1개 유지. `Site`에 원청(clientOrgId) 연결 + **override 테이블**(현장×서류 요구 세트, 계획서 DOCX 템플릿 선택, 안전 설정=3.4, **반입검사·건강검진 재실시 주기**=3.6)을 헬퍼 서비스에서 병합. 전역 junction은 기본값으로 존속 → 기존 소비처(Compliance/WorkPlan) 무손상.

### 3.8 ★현장 즉시 도입 경로 — "기통과" 소급 체크 (2026-07-17 사용자: "그것이 현장에 바로 사용 가능한 거야")

**문제**: 프로그램 도입 시점에 이미 투입 중인 차량·인력은 현실에서 심사·반입검사·교육을 전부 통과한 상태 — 시스템이 디지털 파이프라인 재통과를 강요하면 첫날부터 못 씀.

**대전제(2026-07-17 사용자 확정)**: **BP가 프로그램을 쓴다는 보장이 없다** → 공급사(장비업체) 단독으로도 전 과정이 완결돼야 함. 투입 승인 원칙은 "BP 승인"이되, BP 미사용 시 **구두 승인을 공급사가 기록**하고 투입할 수 있어야 한다.

**투입 확정 경로 3가지**:
| 모드 | 경로 | 라벨 |
|---|---|---|
| BP 프로그램 사용 | BP 인앱 승인(P0 심사→승인) | 정상 승인 |
| BP 사용 + 기투입 자원 | BP 소급 **일괄 승인** | 소급 승인 |
| **BP 미사용(공급사 단독)** | 공급사가 **[구두 승인 기록]**(승인자명·일시·메모 선택) 후 임의 투입 | **구두 승인** |

- 공급사 단독 모드에서도 되는 것: 자원·서류·만료 관리 / 투입 관리 / **일일 확인서**(BP 인앱 서명 대신 **종이 전표 사진 첨부**로 갈음) / 월간 집계 / 정산 — 공급사 혼자서 완결.
- 구두 승인 건은 기록 로그(누가·언제 기록)가 자동 보존(분쟁 대비). BP가 추후 프로그램에 합류하면 구두 승인 건을 소급 승인으로 전환 가능.

**설계**: ① 공급사(장비업체)가 자원 단위로 **[기통과 완료 체크]**("이 현장에 심사·반입검사·교육·검진 이미 완료" 자가 신고, 현재 작업 중 표시) → ② **BP가 목록으로 받아 일괄 승인**(자원별 체크박스, 한 화면에서 끝 — 신규 파이프라인 재통과 없음) → ③ 승인 즉시 투입 상태 → 오늘부터 일일 확인서·정산 가동.
- 완료일자(반입검사일·교육일·검진일)는 **선택 입력** — 적으면 주기 관리(월1회·6개월 재검 알림)가 그날부터 이어짐, 비우면 승인일 기준.
- 소급 건은 **"소급 승인" 라벨**로 정상 파이프라인 통과 건과 구분 표시(감사 추적).
- L2a의 "현장 내 기검증 자원 풀"에 소급 건 포함 → 교체 후보로도 즉시 사용.
- **첫날 사용 시나리오**: 자원 등록(서류는 있는 만큼) + 기통과 체크 + 계약 단가 등록 → 투입 → 일일 확인서 기록 + BP 서명 → 월간·정산. 견적·심사 디지털화는 그 다음 건부터 자연 유입.

## 4. IA 재편 — 역할당 6허브 (⑥ 안전 별도 — 2026-07-17 확정)

| 허브 | BP (27→5) | 공급사 (22→5) | ADMIN (26→5+관리) |
|---|---|---|---|
| ① 대시보드 | 현장 상황판+알림 | 대시보드+알림 | 운영 현황 |
| ② 견적 | 요청·수신·비교선정 | 입찰·제안·발송·수신함 | 견적 관리 |
| ③ 서류 | **받은 심사(원스톱)**·만료·수집·이행 | 서류 허브(현행 통합 유지)·심사 보내기 | 검토큐·종류/요구세트 정책 |
| ④ 현장 운영 | 계획서·투입(대기/현황/장비/인원 통합)·검사·출근·확인서·공지·안전 | 자원(장비·인력)·투입·파이프라인·일정 | 현장·계획서·안전 |
| ⑤ 정산 | 월간확인서·명세서 승인 | 투입 정산·월간확인서·통계 | 정산 관리 |
| ⑥ 안전 | 알림설정(온도·휴식)·관제(워치·알림)·안전점검 | 자사 작업자 안전알림·점검 | 안전 정책·관제 |

- BP의 `내 장비/공급사 장비/내 인원/공급사 인원` 4메뉴 → **1화면+범위 필터**로 흡수 (`Sidebar.tsx:105-108` 중복 제거).
- 상세 라우트→허브 매핑표는 실사 리포트 E항 그대로 채택(App.tsx ~72라우트 전수).
- 회사/사용자/원청 관리는 ADMIN "관리" 그룹으로 별도(허브 외).

## 5. 안전·현장관리 추가 기능 — 전문가 제안 (2026-07-17 확정판)

**설계 철학**: "계획서에 적은 약속(위험 6행·풍속기준·유도원 배치)을 시스템이 매일 대조·감시하게 하자." 우선순위 = 치명 리스크 직결 × 기존 인프라 재사용(저비용).

**1순위 (2026-07-17 사용자 조정 반영)**
| # | 기능 | 근거 | 구현 |
|---|---|---|---|
| S1 | **강풍 작업중지 자동 경보** | 고소·크레인 전도/붕괴. 계획서 "10m/s 중지"가 종이 약속뿐 | KMA 연동 재사용(폭염 엔진 쌍둥이). 임계 초과 시 작업자+BP+공급사 동시 경보. 현장 프로필 임계 |
| S2′ | **★안전점검원 법정점검 NFC 강제** (신규, 사용자) | 법정 일일점검을 실제론 작업자가 일괄 체크하고 점검원은 서명만 하는 형식화 문제 | **장비 NFC 직접 태그해야 체크리스트 오픈**(현장 방문 증명: 태그 시각·단말 기록) → 템플릿 항목 체크(★사용자가 템플릿 제작 예정) → 점검원 서명 → 저장. **조종원 일일점검과 별도 트랙**(2점검 체제). 기존 nfcTagId·점검 인프라 재사용. **수행 주체 = 안전점검회사 소속 점검원(§1.1, 회사유형·인력 신설과 한 묶음)** |
| S3 | **일일점검 미완료 작업시작 차단** | 조종원 점검(기존 NFC 일일점검) 강제력 없음 | 기존 일일점검 + 작업시작 게이트 1개(현장 프로필: 차단/경고) |
| S4′ | **★가동시간→정비 일정 관리** (신규, 사용자) | 정비 시기를 감으로 관리 | P0.5b 일일 확인서의 시간 데이터 **누적 가동시간** 집계 → 정비주기 도달 알림(Equipment oil_change_due·정비이력 테이블 기존 재사용) |

- ~~유도원 미배치 경보~~ **폐기(사용자)**: 현장에서 유도원 없으면 관리자가 작업을 안 시킴 — 프로그램 확인 불요.

**S5′ ★알림 3등급 + TTS 읽어주기 + 확인응답 루프 (2026-07-18 사용자 제안·전문가 설계)** — 작업 중이라 못 읽고, 알림음 없이는 인지 못하는 문제:
| 등급 | 예 | 전달 | 기본 |
|---|---|---|---|
| 긴급 | 작업중지(38℃·강풍)·SOS | **TTS+최대음량 강제+반복+긴 진동, 무음모드 우회**(Android 알람 채널) | 항상 ON(불가끔) |
| 주의 | 휴식(33℃)·수분 | TTS 1회+진동+**워치 손목진동** | ON(끄기 가능) |
| 일반 | 공지·만료 D-30 | 일반 알림음, 읽어주기 opt-in | OFF |
- 원칙: 안전 알림은 opt-in 금지(설정 안 만진 작업자가 제일 위험). TTS 문구는 짧은 정형("휴식 시간입니다. 20분 쉬세요").
- **확인응답 루프**: 알림 후 [확인] 대형 버튼 — 미응답 5분 재알림(증폭) — 지속 미응답 시 관제 "휴식 미확인 ○○○" 표시(관제 위젯과 연동). 전달이 아니라 **인지**를 관리.
- 구현: 폰앱 네이티브 TTS API(저비용) + FCM data 메시지 등급 필드 + 워치 채널 재사용. P3 안전 웨이브 폰앱 파트에 포함.

**★안전 증거 사슬 원칙 (2026-07-18 사용자 확정 — P3 전체 관통 설계 원칙)**
사고 시 질문은 "사업주가 **명확히 고지하고 잘 관리했음을 증명**할 수 있는가"(중대재해처벌법·산업안전보건법). 안전 기능은 전부 **타임스탬프 증거로 기록**되어야 한다:
- 알림: 발송→단말 수신→TTS 재생→[확인] 탭(누가·언제)→미확인 재알림→관제 표시→관리자 조치 — **전 단계 기록**.
- NFC 법정점검(S2′): 태그 시각·단말·점검원·항목체크·서명 = "실제 현장에서 점검했다" 물증.
- 작업계획서 5서명·TBM·일일점검·휴식 준수 이력 — 동일 원칙.
- **증빙 출력**: 현장·기간별 "안전관리 이행 보고서"(고지·확인·조치 이력 일괄)를 조회·인쇄 가능하게 — 감사·사고 조사 시 즉시 제출. 이것이 BP가 이 프로그램을 쓰는 법적 이유가 됨.

**2순위 (안전문화·관리 체감)**: S4 강우 후 지반확인 서명 강제(침하·전도, 날씨 데이터 재사용) / S5 TBM 매일 서명(특별교육 서약의 일일판, 미확인자 실시간) / S6 SOS 앱 버튼+무응답 체크(워치 응급 채널 재사용, 1인 작업 대비) / S7 현장 상황판(장비·출근·미점검·기상단계·미서명 — IA ①과 동시) / S8 기사 셀프 만료 D-day 뷰(데이터 노출만) / S9 응급 정보 카드(현장별 최기 응급실·비상연락망, 응급 시 관제 표시) / S10 지오펜스 자동 출입기록(geofenceRadiusM 존재) / S11 날씨 아침 브리핑 / S12 만료 D-30/D-7 알림톡.

**보류 권고 (전문가 판단·정직 표기)**: UWB/비콘 접근경보(하드웨어 필요 — GPS 흉내는 오경보로 무용화), 바이탈 기반 작업 차단(의료정보 법적 리스크 — 권고 알림까지만), PTW 작업허가(SK 자체 시스템 중복 가능성 — 사용자 확인 전 보류).

## 6. 구현 로드맵 (오퍼스 작업 패키지)

- **P0 — BP 원스톱 심사** (§3.1 + 메일 CC): 문서목록 API → 열람권한 분기 → 상태머신 → 뷰어/승인 UI 이식 → 승인 후속 액션(계획서 프리필+점검요청 다이얼로그) → CC. *효과 즉시 체감, 범위 국소.* (진행중)
- **P0.5 — 현장 즉시 도입**(§3.8 + §3.3.1 + §3.6.3) — *도입 첫날 실사용.* **승인됨(2026-07-17), 2웨이브 분할**:
  - **P0.5a (선행)**: ①계약 등록(V97 — 공급사·BP(회사 or 이름만·단독모드)·현장·장비규격·기본단가(일대/월대)+**OT 5분류 단가**·기간·계약서 파일·메모, 공급사 CRUD+BP 조회) ②기통과 소급(`site_resource_onboarding`: 현장×자원, 완료일자 3종 선택입력, REQUESTED→BP 일괄 APPROVED / **VERBAL**=공급사 구두승인 기록 즉시확정) — **승인/확정 시 기존 ResourceCheck 승인행 자동 생성**(장비=반입검사, 인원=건강검진+안전교육, 완료일 반영) → 기존 게이트·readiness·주기관리·L2a 풀이 무수정으로 작동(핵심 설계 결정: 게이트 로직 불가침, 레코드 주입으로 해결) ③심사 메일에 "웹에서 확인" 링크.
  - **P0.5b (후행)**: 일일 확인서(하루 1건: 작업내용·위치·구분·OT 5분류 시간·작업시간) — BP 인앱 서명 or **전표 사진 첨부**(단독모드) → 월간 원장 뷰(Site.settlementDay 26~25 주기·OT 5열·일별 서명) + 기간 합계(계약 단가 × 시간) + **작업자 "내 작업내역" 뷰**(본인 일일/월간 기록·서명 상태·OT 확인 — 모바일웹, UX 대원칙 §0-A). SettlementCalculator 5분류 확장은 P2에서.
- **P1 — ★작업계획서 자동화** (§3.6 L1→L2→L3): 자동 생성 → 자원 교체 델타 → 교체 사전검증. *락인 해제 = 제품 핵심 가치.* SK 실양식 수령 후 DOCX 매핑 확정.
- **P2 — IA 6허브 재편 + 견적 템플릿(5분류 단가) + 투입-정산 디커플링 + ★원청(SK) 통합 모니터링 허브**(§1.1 — 원청 역할 신설·현장 관제 대시보드 읽기전용 + **인원 분포·시간대별 혼잡도 위젯**(§0-A#4, 구역 단위 — 점심시간 조정 등 현장 계획 데이터), IA 재편과 한 묶음) (§4, §3.3, §3.2). **P2 검수 기준(2026-07-17 사용자 강조 — 디자인 중심 검수)**: ①메뉴 간소화 실측 — 합칠 건 합치고 서브메뉴로 내려 **역할당 최상위 6허브 이하**, 현재 BP 27+/공급사 22 → 목표 달성 수치 보고 ②페르소나 "3초 테스트" — 각 역할이 오늘 할 일을 첫 화면에서 3초 내 인지하는가 ③대시보드 차트 품질 — 표 나열 금지, 카드·차트 한눈 파악(§0-A) ④디자인 일관성·모바일 390 — 스크린샷 전수 판정. 미달 시 재작업 필수(비차단 없음 — IA 웨이브에선 디자인이 곧 기능).
- **P3 — 안전 웨이브**: S1 강풍경보 + **S2′ NFC 법정점검(+안전점검회사 유형·점검원 관리 §1.1)** + S3 점검게이트 + S4′ 가동시간→정비 + 현장 프로필·안전 현장설정 + 저비용 아이디어(SOS·지오펜스·브리핑·만료 알림톡) (§3.4, §3.5, §5).
- **외부(사용자 액션)**: 다온톡 콘솔에 안전교육·반입검사 날짜통보 템플릿 등록(코드만으론 불가).

## 7. 결정 기록 (2026-07-17 사용자 확정)

1. **초안 승인 — P0부터 오퍼스 구현 착수.**
2. **반입검사 = 투입 게이트**: 장비=반입검사 완료 시 투입가능, 인력=건강검진+안전교육 완료 시 투입가능. 기존 ResourceCheck(VEHICLE_SAFETY/HEALTH_CHECK/SAFETY_TRAINING)+readiness 구조 **재사용**(불편 없으면 재사용 원칙), 노출 명칭 "반입검사"·게이트 연결 검수.
3. **견적 = 템플릿(단가표) 방식** (§3.3). 실양식 수령 후 필드 확정.
4. **안전 별도 허브** — 총 6허브.
5. **교체 규칙 확정**: 같은 현장+기검증 자원=업체변경 신청서만 / 다른 관리현장=전부 신규 / 반입검사 월1회·건강검진 6개월 재실시(주기 현장별 BP 설정) / 교체 시점=당일 긴급·사전 계획·신규 업체 추가 모두.
6. **견적 디커플링 확정**: 전체 연결 유지 + 서류심사부터 시작 1급 동선 + 견적서=선정 근거자료 보존.
7. 대기: 사용자 실양식(**작업계획서·업체변경 신청서**·견적서·월간확인서 등) 제공 → P1 설계 입력.
